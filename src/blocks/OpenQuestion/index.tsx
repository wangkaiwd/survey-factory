interface OpenQuestionProps {

}

const OpenQuestion = (props: OpenQuestionProps) => {
  console.log('OpenQuestion', props)
  return (
    <div>
      OpenQuestion
    </div>
  )
}

export default OpenQuestion
